services:
  - type: web
    name: my-voice-assistant
    runtime: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn main:app
    envVars:
      - key: GOOGLE_API_KEY
        sync: false
